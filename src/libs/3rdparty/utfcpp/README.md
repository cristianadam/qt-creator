# utfcpp

Header-only UTF-8 handling, https://github.com/nemtrif/utfcpp, version 4.0.8.

Imported from the upstream `source/` directory. Used by `../cxx-frontend`,
which is the only consumer; the version matches the one that frontend's own
build fetches.

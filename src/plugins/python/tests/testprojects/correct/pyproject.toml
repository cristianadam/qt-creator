[project]
name = "CorrectTest"
description = "pyproject.toml project example"
version = "0.1"
dependencies = ["PySide6>6.8"]
requires-python = ">=3.9"

[tool.pyside6-project]
files = ["main.py", "folder/file_in_folder.py"]
